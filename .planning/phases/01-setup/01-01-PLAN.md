---
phase: 01-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/App.css
  - src/index.css
autonomous: true

must_haves:
  truths:
    - "User can open localhost:5173 in browser and see a page render"
    - "React app renders without console errors"
    - "Editing src/App.tsx triggers hot reload without manual refresh"
  artifacts:
    - path: "package.json"
      provides: "Project definition with React and Vite dependencies"
      contains: "react"
    - path: "vite.config.ts"
      provides: "Vite build configuration"
      contains: "defineConfig"
    - path: "index.html"
      provides: "HTML entry point"
      contains: "root"
    - path: "src/main.tsx"
      provides: "React DOM mount point"
      contains: "createRoot"
    - path: "src/App.tsx"
      provides: "Root React component"
      exports: ["App"]
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "script module tag"
      pattern: "src/main.tsx"
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "import and render"
      pattern: "import.*App"
---

<objective>
Initialize a React + Vite + TypeScript web application that runs in the browser.

Purpose: Establish the foundation for the Pomodoro timer demo. This phase creates the scaffolding that Phase 2 (Timer Core) will build upon.

Output: A running React application accessible at localhost:5173 with hot reload working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite React TypeScript project</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tsconfig.node.json
    index.html
    src/main.tsx
    src/App.tsx
    src/App.css
    src/index.css
    src/vite-env.d.ts
  </files>
  <action>
    Create a new Vite project with React and TypeScript template.

    Run: npm create vite@latest . -- --template react-ts

    This scaffolds:
    - package.json with react, react-dom, vite, typescript dependencies
    - vite.config.ts with React plugin
    - tsconfig.json and tsconfig.node.json for TypeScript
    - index.html with root div and module script
    - src/main.tsx mounting React to #root
    - src/App.tsx with starter component
    - CSS files for basic styling

    The --template react-ts flag ensures TypeScript is configured out of the box.
    Use "." to create in current directory (project already has .planning folder).
  </action>
  <verify>
    Verify files exist:
    - ls package.json vite.config.ts src/main.tsx src/App.tsx

    Verify package.json contains react:
    - grep "react" package.json
  </verify>
  <done>Project scaffolded with all Vite + React + TypeScript files in place</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and verify dev server</name>
  <files>
    package-lock.json
    node_modules/
  </files>
  <action>
    Install npm dependencies and start the development server to verify the app runs.

    Run: npm install
    Run: npm run dev (in background, verify it starts)

    The dev server should:
    - Start on port 5173 (Vite default)
    - Serve the React app
    - Enable hot module replacement (HMR)

    After verifying the server starts successfully, curl localhost:5173 to confirm HTML is served.
  </action>
  <verify>
    - npm run dev starts without errors
    - curl http://localhost:5173 returns HTML containing "root" div
    - No TypeScript or React errors in terminal output
  </verify>
  <done>Dependencies installed, dev server runs, app accessible at localhost:5173</done>
</task>

<task type="auto">
  <name>Task 3: Clean up boilerplate for minimal starting point</name>
  <files>
    src/App.tsx
    src/App.css
    src/index.css
  </files>
  <action>
    Simplify the default Vite template to a minimal starting point for the timer.

    Update src/App.tsx:
    - Remove the counter state and logo imports
    - Replace with a simple component that renders "Pomodoro Timer" heading
    - Keep the basic structure but remove Vite boilerplate

    Update src/App.css:
    - Remove Vite-specific styles (logo animations, etc.)
    - Keep minimal useful styles or empty file

    Update src/index.css:
    - Keep basic reset/normalize styles
    - Remove Vite-specific styling

    Delete src/assets/ folder if it exists (contains Vite logo).

    The goal is a clean slate that shows "Pomodoro Timer" when loaded.
  </action>
  <verify>
    - src/App.tsx contains "Pomodoro Timer" text
    - No references to Vite logos or counter state in App.tsx
    - curl localhost:5173 and verify response (or check browser if dev server running)
  </verify>
  <done>Boilerplate cleaned, App.tsx shows "Pomodoro Timer" heading, ready for Phase 2</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run `npm run dev` and verify server starts on port 5173
2. Curl or browse to http://localhost:5173
3. Verify page shows "Pomodoro Timer" heading
4. Verify browser console has no React errors
5. Edit src/App.tsx, change text, verify hot reload updates browser without manual refresh
</verification>

<success_criteria>
- [ ] `npm run dev` starts without errors
- [ ] Browser shows "Pomodoro Timer" at localhost:5173
- [ ] No console errors in browser
- [ ] Hot reload works (change App.tsx, see update without refresh)
- [ ] Requirements INFRA-01 and INFRA-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup/01-01-SUMMARY.md`
</output>
